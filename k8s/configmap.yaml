apiVersion: v1
kind: ConfigMap
metadata:
  name: e2e-test-coverage-config
  namespace: e2e-test-coverage
  labels:
    app.kubernetes.io/name: e2e-test-coverage
data:
  GIN_MODE: "release"
  # HOST env var is intentionally omitted to run the server on port 8080 (HTTP)
